# 使用者流程（端對端）

## 總覽流程圖
```mermaid
flowchart TD
  A["① 輸入直播素材<br/>影片 + 彈幕(jsonl) + 字幕(srt)"] --> B["② Ingest<br/>探測/抽幀/抽音/載入彈幕字幕"]
  B --> C["③ 多模態訊號分析<br/>音訊·語音·彈幕·視覺 → 9 條訊號"]
  C --> D["④ 融合與高光偵測<br/>加權融合 → 峰值 → 高光視窗 + 歸因"]
  D --> E["⑤ 內容審核<br/>視覺/語音/文字三模態掃描"]
  E -->|通過/消音| F["⑥ AI 自動剪輯<br/>裁切·重構圖·字幕·鉤子"]
  E -->|違規| X["⛔ 阻擋並送人工複審"]
  F --> G["⑦ 多平台產出<br/>TikTok / Reels / Shorts + 精華合輯"]
  G --> H["⑧ 成效報告 + result.json<br/>省時% · 品質分 · 審核狀態"]
```

## 時序（Web Live Demo）
```mermaid
sequenceDiagram
  participant U as 使用者
  participant W as Web UI
  participant P as Pipeline (背景執行緒)
  U->>W: 選擇素材/上傳、勾選平台、開始分析
  W->>P: POST /api/jobs
  loop 每 0.5s 輪詢
    W->>P: GET /api/jobs/{id}
    P-->>W: {stage, pct, msg}
  end
  P-->>W: {state:done, result}
  W-->>U: 分數曲線 + 短片 + 審核報告（可線上播放/下載）
```

## 三種操作路徑
### A. CLI（適合自動化 / 排程）
```bash
sparkreel analyze stream.mp4 --chat chat.jsonl --subtitles subs.srt --platforms tiktok,reels
```
即時進度條 → 彩色分析報告 → `assets/output/<job>/` 產物。

### B. 批量（適合整季 VOD / 多頻道）
```bash
sparkreel batch ./vods/ --workers 4
```
自動偵測每支影片的 sidecar 彈幕/字幕，並行處理，逐檔回報。

### C. Web（適合展示 / 非技術使用者）
`sparkreel serve` → 瀏覽器上：選素材 → 一鍵分析 → 即時進度 → 互動式結果（曲線 hover、平台切換、下載）。

## 輸入格式
- **影片**：mp4 / mov / mkv / flv / ts …（ffmpeg 可讀者皆可）
- **彈幕**：jsonl / json / csv，欄位彈性（`t/time/timestamp` + `user` + `text/message`），
  時間可為秒數或 `hh:mm:ss`
- **字幕**：srt / vtt（生產環境由 Amazon Transcribe 產生）
